name: Cerebras API

on:
  workflow_dispatch:

  schedule:
    - cron: "0 0 * * *"

jobs:
  ai-studio:
    uses: ./.github/workflows/provider-template.yml

    with:
      provider-name: ai-studio
      test-file: tests/providers/test_cerebras.py

    secrets:
      secret_1: ${{ secrets.CEREBRAS_API_KEY }}